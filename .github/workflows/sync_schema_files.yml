name: Sync JSON Schema Files from MaaFramework

on:
  schedule:
    - cron: '10 0 * * *'  # UTC 00:10 每天运行
  workflow_dispatch:

permissions:
  contents: write

jobs:
  check-repository:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}
    steps:
      - id: check
        run: |
          if [[ "${{ github.repository }}" == "overflow65537/MAA_SnowBreak" ]]; then
            echo "Repository is overflow65537/MAA_SnowBreak, proceeding with sync"
            echo "should_run=true" >> $GITHUB_OUTPUT
          else
            echo "Repository is not overflow65537/MAA_SnowBreak, skipping sync"
            echo "should_run=false" >> $GITHUB_OUTPUT
          fi

  sync-schema-files:
    needs: check-repository
    if: needs.check-repository.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Repository
        uses: actions/checkout@v6

      - name: Set Up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: Download JSON schema files from MaaFramework
        run: |
          set -euo pipefail
          echo "Current Date and Time (UTC): $(date -u '+%Y-%m-%d %H:%M:%S')"
          mkdir -p tools/schema
          curl -fsSL -o tools/schema/interface.schema.upstream.json https://raw.githubusercontent.com/MaaXYZ/MaaFramework/main/tools/interface.schema.json
          curl -fsSL -o tools/schema/interface_config.schema.json https://raw.githubusercontent.com/MaaXYZ/MaaFramework/main/tools/interface_config.schema.json
          curl -fsSL -o tools/schema/interface_import.schema.json https://raw.githubusercontent.com/MaaXYZ/MaaFramework/main/tools/interface_import.schema.json
          curl -fsSL -o tools/schema/pipeline.schema.json https://raw.githubusercontent.com/MaaXYZ/MaaFramework/main/tools/pipeline.schema.json
          python tools/merge_interface_schema.py --base tools/schema/interface.schema.upstream.json --custom tools/schema/interface.schema.custom.json --output tools/schema/interface.schema.json
          rm -f tools/schema/interface.schema.upstream.json
          echo "Checking downloaded files:"
          ls -la tools/schema/*.schema.json

      - name: Check for changes with git status
        id: check_status
        run: |
          schema_files=(
            tools/schema/interface.schema.json
            tools/schema/interface_config.schema.json
            tools/schema/interface_import.schema.json
            tools/schema/pipeline.schema.json
          )
          git add -f "${schema_files[@]}"
          if [[ -n "$(git status --porcelain -- "${schema_files[@]}")" ]]; then
            echo "Changes detected in JSON schema files"
            git status --porcelain -- "${schema_files[@]}"
            echo "changes_detected=true" >> $GITHUB_OUTPUT
            if [[ -n "$(git status --porcelain -- tools/schema/interface.schema.json)" ]]; then
              echo "interface_changed=true" >> $GITHUB_OUTPUT
            else
              echo "interface_changed=false" >> $GITHUB_OUTPUT
            fi
            if [[ -n "$(git status --porcelain -- tools/schema/interface_config.schema.json)" ]]; then
              echo "interface_config_changed=true" >> $GITHUB_OUTPUT
            else
              echo "interface_config_changed=false" >> $GITHUB_OUTPUT
            fi
            if [[ -n "$(git status --porcelain -- tools/schema/interface_import.schema.json)" ]]; then
              echo "interface_import_changed=true" >> $GITHUB_OUTPUT
            else
              echo "interface_import_changed=false" >> $GITHUB_OUTPUT
            fi
            if [[ -n "$(git status --porcelain -- tools/schema/pipeline.schema.json)" ]]; then
              echo "pipeline_changed=true" >> $GITHUB_OUTPUT
            else
              echo "pipeline_changed=false" >> $GITHUB_OUTPUT
            fi
          else
            echo "No changes detected in JSON schema files"
            echo "changes_detected=false" >> $GITHUB_OUTPUT
            echo "interface_changed=false" >> $GITHUB_OUTPUT
            echo "interface_config_changed=false" >> $GITHUB_OUTPUT
            echo "interface_import_changed=false" >> $GITHUB_OUTPUT
            echo "pipeline_changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Commit and Push Changes
        if: steps.check_status.outputs.changes_detected == 'true'
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          timestamp=$(date -u "+%Y-%m-%d %H:%M:%S")
          changed_files=""
          if [[ "${{ steps.check_status.outputs.interface_changed }}" == "true" ]]; then
            changed_files+="interface "
          fi
          if [[ "${{ steps.check_status.outputs.interface_config_changed }}" == "true" ]]; then
            changed_files+="interface_config "
          fi
          if [[ "${{ steps.check_status.outputs.interface_import_changed }}" == "true" ]]; then
            changed_files+="interface_import "
          fi
          if [[ "${{ steps.check_status.outputs.pipeline_changed }}" == "true" ]]; then
            changed_files+="pipeline "
          fi
          changed_files=$(echo "$changed_files" | xargs)
          git commit -m "chore: update $changed_files schema files from MaaFramework - $timestamp UTC"
          git push
          echo "JSON schema files successfully synchronized"

      - name: No changes detected
        if: steps.check_status.outputs.changes_detected != 'true'
        run: echo "No changes detected in JSON schema files"
